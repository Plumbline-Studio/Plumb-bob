Human step: `cp workflows-to-install/*.yml .github/workflows/` — agent tokens can't write workflow files. These two workflows (`ci.yml`: typecheck + offline test tier + deterministic mock-tier harness on every PR and push to main; `live-eval.yml`: real-model harness runs, weekly and on demand, skipping gracefully when the `ANTHROPIC_API_KEY` secret is absent) were authored alongside this engine but the GitHub App token used to push this branch is not permitted to create or update files under `.github/workflows/`, so they are staged here verbatim for a human to move into place and delete this directory.
